@model IEnumerable<ASM2_KSTH.ViewModels.StudentViewModel>

@{
    ViewData["Title"] = "Add Student";
    Layout = "~/Views/Shared/LayoutAdmin.cshtml";
}

<h2>Add Student to Class</h2>



<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table id="example2" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Student ID</th>
                        <th>Name</th>
                        <th>Class Name</th>
                        <th>Course Name</th> <!-- Sửa Major thành Course Name -->
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var student in Model)
                    {
                        <tr>
                            <td>@student.StudentId</td>
                            <td>@student.Name</td>
                            <td>@student.ClassName</td>
                            <td>@student.CourseName</td> <!-- Sửa MajorName thành CourseName -->
                            <td>
                                <a asp-action="Add" asp-route-id="@student.StudentId" asp-route-studentId="@student.StudentId">
                                    <button type="button" class="btn btn-outline-dark px-4 d-flex gap-2 me-2">
                                        <i class="material-icons-outlined">cloud_upload</i> Add Class
                                    </button>
                                </a>
     
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
         
            <p>
                <a asp-controller="Classes" asp-action="index"><button type="button" class="btn btn-warning px-5">Back</button></a>
            </p>
        </div>
    </div>

    @section scripts {
        <script>
            $(document).ready(function () {
                var table = $('#example2').DataTable({
                    lengthChange: false,
                    buttons: ['copy', 'excel', 'pdf', 'print']
                });

                table.buttons().container()
                    .appendTo('#example2_wrapper .col-md-6:eq(0)');
            });
        </script>
    }
</div>
